#pragma once

#include <juce_core/juce_core.h>

//==============================================================================
/**
    8.284：**ドラムマップのプリセット**を、窓を出さずに確かめる（Phase 277）。

    ```
    "Manta Studio.exe" --drummap-selftest
    ```

    ### なぜ機械に数えさせるのか

    読み込みには**「やらないこと」の決まり**があります（`DrumMapPresets.h`の表）：

    - プリセットに無い行を**消さない**（消すと、その音のノートが画面から消えます。1.9）
    - **ミュートは持ち運ばない**（読んだ瞬間に鳴らない行ができる）

    どちらも**やってしまっても、その場では何も起きません**。
    気づくのは「打ち込んだはずのノートが見当たらない」ときで、
    そこからプリセットを疑うのは難しい。**番号で押さえておくところ**です（8.4）。

    ファイルの読み書きも通ります（保存→読み込みの往復）。

    **使い終わったプリセットは消します。** 本人のプリセット一覧に
    試験用の名前が残らないよう、名前も紛れないものにしてあります。

    **問題の数を終了コードで返します**（0なら合格）。
*/
namespace DrumMapPresetSelfTest
{
    /** コマンドラインに`--drummap-selftest`があれば走らせて、trueを返す。 */
    bool runIfRequested (const juce::String& commandLine);
}
