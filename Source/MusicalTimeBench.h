#pragma once

#include <juce_core/juce_core.h>

//==============================================================================
/**
    8.137：**動作の重さを測る**（Phase 175／8.1のE1・8.105の宿題3）。

    ### なぜ要るか

    8.105の宿題3で、ノートの時刻は**拍で保存**されるようになります。すると
    `Note::getStartTime()`は「保存されている拍を、そのときのテンポで秒へ直す」
    という仕事に変わります——**プロパティを1つ読むだけ**だったものが、
    **テンポの表を毎回引く**ようになるということです。

    描画は1フレームでノートを何千回も引きます。**遅くなるとしたら、ここです。**

    > 8.1のE1（「全体的に動作がもっさりしている」）は
    > **「最初にやるのは計測です」**と書いたまま、ずっと保留になっていました。
    > **動きが1つも変わっていない今**が、比べるための値を取る最後の機会です。

    ### 使いかた

    ```
    PersonalDAW.exe --measure-musical-time
    ```

    画面を出さずに測って、結果をファイルへ書いてから終了します
    （書き出し先は結果ファイルの1行目にも入っています）。

    **Releaseビルドで測ること。** JUCEはDebugだと描画も走査も目に見えて遅く、
    Debugの値どうしを比べても意味のある差になりません（8.1のE1）。

    ### 何と比べるのか

    | 測るもの | いまの意味 | 宿題3のあと |
    |---|---|---|
    | `getStartTime()` | プロパティを1つ読む | **拍から換算する** |
    | `getStartBeats()` | **拍から換算する** | プロパティを1つ読む |

    **いまの`getStartBeats()`が、宿題3のあとの`getStartTime()`です。**
    つまり今日この2つを並べて測れば、**変えたあとの重さが今日わかります。**

    テンポの変化点の数を変えながら測るのは、`TempoMap`の走査が
    **変化点の数に比例する**ためです（`TempoMap.h`）。
    **索引が要るかどうかは、この表を見て決めます**（8.105のPhase 152）。
*/
namespace MusicalTimeBench
{
    /** コマンドラインに`--measure-musical-time`があれば測って、trueを返す。

        **trueならメインウィンドウを作らないこと**（サンドボックスのワーカーと同じ扱い）。 */
    bool runIfRequested (const juce::String& commandLine);
}
