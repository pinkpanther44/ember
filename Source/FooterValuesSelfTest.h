#pragma once

#include <juce_core/juce_core.h>

//==============================================================================
/**
    8.268：**フッターが映す値と、書き換える先**を、窓を出さずに確かめる（Phase 270）。

    ```
    "Manta Studio.exe" --footer-selftest
    ```

    ### なぜ機械に数えさせるのか

    フッターのBPM・キー・拍子は、**再生カーソルの位置で効いている値**を映し、
    **その値を書き換えます**。振り分けを間違えると：

    - 曲の途中の120を見ながら打ち込んだのに、**曲頭の145が変わる**
    - カーソルの拍で変化点を置いてしまい、**同じ値の札がレーンに増える**

    どちらも**音では気づけません**し、画面でも「変えたつもりの場所が変わっていない」
    としか見えません。**番号で押さえておくところ**です（8.4）。

    `TempoMap`／`KeyMap`側の「どれが効いているか」は`Tools/TempoMapTests`が見ています。
    こちらが見るのは**その上に載っている`ProjectModel`の振り分け**です。

    **問題の数を終了コードで返します**（0なら合格）。
*/
namespace FooterValuesSelfTest
{
    /** コマンドラインに`--footer-selftest`があれば走らせて、trueを返す。 */
    bool runIfRequested (const juce::String& commandLine);
}
