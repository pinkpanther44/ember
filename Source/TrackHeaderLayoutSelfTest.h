#pragma once

#include <juce_core/juce_core.h>

//==============================================================================
/**
    8.295：**トラックヘッダーの並びを、窓を出さずに確かめる**（Phase 288／改善案1）。

    ```
    "Manta Studio.exe" --header-selftest
    ```

    ### なぜ機械に数えさせるのか

    Phase 288でヘッダーを組み替えました（本人の指定）：

    - ボタンを**1.5倍**（15→23px。●・オートメーション・ソロ・ミュート・「i」）
    - **「i」を名前の左へ**、空いた右端に**種類の絵**
    - IN（入力モニター）も1.5倍にして、絵の左隣へ

    **場所を取るものが増えて、大きくなった**ということです。詰まると出るのは

    - 隣のボタンと**当たり判定が重なる**（押したつもりで別のものが動く）
    - 名前が**見切れる**、あるいは幅が0になって消える
    - メーターに重なって、**押すたびにピークがリセットされる**（8.65で1度踏んだ形）

    のどれかで、**どれも画面では「なんとなく詰まっている」としか見えません。**
    しかも出るのは**狭いヘッダー**（190px）や**深いフォルダ**（1階層12pxの字下げ）
    といった端のほうだけで、普段の幅では気づけません。

    ### 見るもの

    | | |
    |---|---|
    | 重なり | 出ているもの同士が**1pxも重ならない**こと（`expanded()`ぶんも含める） |
    | 見切れ | どれもヘッダーの幅の内側にあること。**名前の幅が残っている**こと |
    | 並び順 | 左から ［三角］［i］［名前］［IN］［絵］［メーター］（本人の指定どおりか） |
    | 大きさ | ボタンが**1.5倍になっている**こと（22px以上。指定は約1.5倍） |

    **幅は190（下限）と230（既定）と520（上限）で、字下げは0〜3階層**で回します。
    種別も**7つ全部**（Audio／MIDI／Send／Folder／Chord／VCA／DrumOut）。

    > **画面を作りません。** `TimelineComponent`を1つ作って位置を聞くだけです
    > （`getHeaderRowLayout()`）。**位置は画面が使っているものそのもの**で、
    > ここで数字を組み直してはいません——組み直したら、
    > 「検査は通るのに画面はずれている」が作れてしまいます（1.27）。

    **問題の数を終了コードで返します**（0なら合格）。
*/
namespace TrackHeaderLayoutSelfTest
{
    /** コマンドラインに`--header-selftest`があれば走らせて、trueを返す。 */
    bool runIfRequested (const juce::String& commandLine);
}
