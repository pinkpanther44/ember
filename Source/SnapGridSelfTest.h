#pragma once

#include <juce_core/juce_core.h>

//==============================================================================
/**
    8.271：**刻み（スナップ）の寄せ先**を、窓を出さずに確かめる（Phase 272）。

    ```
    "Manta Studio.exe" --snap-selftest
    ```

    ### なぜ機械に数えさせるのか

    3連符を足したので（本人の要望）、刻みの種類が6つから10へ増えました。
    3連は**2/3を掛けた値**で、間違えても画面では

    - 「なんとなく、3等分になっていない気がする」
    - 「テンポを変えた先だけ、少しずれている気がする」

    としか見えません。**音でも気づけません**（3連は元々もたついて聞こえるもの）。
    数値で押さえておくところです（8.4の「耳で分かるもの・分からないもの」）。

    ### 見るもの

    | | |
    |---|---|
    | 綴りの往復 | `snapGridToString()`↔`snapGridFromString()`。**プロジェクトに書かれる文字** |
    | 拍の刻み | `snapGridBeats()`。3連は同じ音価の2/3 |
    | 寄せ先 | `ProjectModel::snapTime()`。**テンポの変化点をまたいでも拍に乗ること** |

    3つ目がこの道具の肝です。刻みを「いまのテンポでの秒数」で持つと、
    **変化点の向こうで拍から外れます**（8.98でそう直した経緯があります）。
    3連は刻みが細かいぶん、そのずれが早く出ます。

    **問題の数を終了コードで返します**（0なら合格）。
*/
namespace SnapGridSelfTest
{
    /** コマンドラインに`--snap-selftest`があれば走らせて、trueを返す。 */
    bool runIfRequested (const juce::String& commandLine);
}
